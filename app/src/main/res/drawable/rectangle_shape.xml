<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Background color -->
    <solid android:color="@color/navyblue" />

    <!-- Border -->
    <stroke
        android:width="2dp"
        android:color="@color/navyblue" />

    <!-- Corner radius (optional, remove if you want sharp edges) -->
    <corners android:radius="30dp" />

    <!-- Padding inside the shape -->
    <padding
        android:left="10dp"
        android:top="10dp"
        android:right="10dp"
        android:bottom="10dp"/>
</shape>
